import './intro.scss';

const Intro = (): JSX.Element => {
    return (
        <div className="intro">
            <span className="intro__logo">MEIJER</span>
        </div>
    );
}

export default Intro;